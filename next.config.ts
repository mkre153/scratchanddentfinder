import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // PARITY: All URLs end with trailing slash (matches scratchanddentlocator.com)
  trailingSlash: true,
}

export default nextConfig
